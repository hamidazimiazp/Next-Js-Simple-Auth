import React from "react";
import styles from "@/styles/auth.module.css";

const SignUp = () => {
  return (
    <div className={styles.wrapper}>
      <form className={styles.form}>200</form>
    </div>
  );
};

export default SignUp;
